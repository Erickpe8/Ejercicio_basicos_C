#include <stdio.h>

int main() {
	// Primero declaramos las variables
	int  miNumero = 10;
	printf("imprimimos el valor de la variable miNumero: %d", miNumero);
	printf("\nAhora imprimiremos la dirección de memoria: %p", &miNumero);
	return 0;
}

